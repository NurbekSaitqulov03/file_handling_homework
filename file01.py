def main(data:str):
    """
    The data is from the file. Return data as a list type.
    Args:
        data: str
    Returns:
        list: return answer
    """
    x = "python"
    i = 0
    while i < len(x):
        return "tamom boldi"
    i += 1
print(main(23, 23))

# Read data from file